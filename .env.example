# Secret key Flask uses to sign session cookies. Generate one with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-value

# Flask debug mode: True for local development only, must be False in production
FLASK_DEBUG=True
